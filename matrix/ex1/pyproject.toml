[tool.poetry]
name = "matrix_analysis"
version = "1.0.0"
description = "Matrix data analysis program"
authors = ["yru"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
numpy = "^1.25"
pandas = "^2.1"
matplotlib = "^3.7"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"